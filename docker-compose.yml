version: '3'
services:
  home_api:
    image: dariusbakunas/home-api:latest
    container_name: home-api
    ports:
      - "9999:80"
    env_file:
      - .env
    volumes:
      - ./instance:/app/instance